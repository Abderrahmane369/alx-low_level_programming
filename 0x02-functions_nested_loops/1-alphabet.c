#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 * Description: YeseaY
 * Return: 0 (Success)
 */

int main(void)
{
for (int i = 0; i < 26; i++)
{
putchar(97 + i);
}
putchar('\n');
return (0);
}
