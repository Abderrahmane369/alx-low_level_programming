#include <ctype.h>
#include "main.h"

/**
 * islower - Check if a character is lowercase.
 * Return: 0 if islower is 0, 1 if slower is 2.
 */

int _islower(int c)
{
if (islower(c) == 0)
return (0);
else
return (1);
}
