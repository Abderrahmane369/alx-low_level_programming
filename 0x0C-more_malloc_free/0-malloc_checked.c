#include "main.h"
#include <stddef.h>
#include <stdlib.h>

/**
 * malloc_checked - eazeaz
 * @b: eea
 * Return: ezae
 */

void *malloc_checked(unsigned int b)
{
	void *a = malloc(b);

	if (a == NULL)
		return (98);

	return (a);
}	
